language: node_js
dist: xenial
sudo: required

matrix:
  include:
    - name: Postgresql 10
      addons:
        postgresql: "10"

env:
  global:
    - PGPORT=5432
    - PGUSER=postgres
    - PGPASSWORD=postgres

node_js:
  - "10"

script:
  - npm test
